overwrite: true
schema:
  - ./graphql/schema.graphql
documents:
  - ./graphql/*.graphql
  - ./graphql/*/*.graphql
generates:
  ./server/gen/graphql-resolver-types.ts:
    plugins:
      - typescript
      - typescript-resolvers
  ./graphql.schema.json:
    plugins:
      - introspection

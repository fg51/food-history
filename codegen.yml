overwrite: true
schema:
  - ./sample.graphql
documents: null
  # - ./graphql/queries/*.graphql
generates:
  ./server/gen/graphql-resolver-types.ts:
    plugins:
      - typescript
      - typescript-resolvers
  ./graphql.schema.json:
    plugins:
      - introspection
